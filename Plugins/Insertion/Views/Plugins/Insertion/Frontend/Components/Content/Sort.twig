{% block product_list_sort %}
    <div class="panel panel--sort flex flex--row flex--center flex--between">
        <div class="result">
            <h3 class="result__text">{{ search.query.count }} {{ i18n('insertion_count') }}</h3>
        </div>
        <div class="sort">
            <select name="product-list-sort" id="product-list-sort" class="form__input">
                <option>Ascending</option>
            </select>
        </div>
    </div>
{% endblock product_list_sort %}
