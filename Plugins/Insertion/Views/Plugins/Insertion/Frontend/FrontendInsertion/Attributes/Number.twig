<div class="form__control">
    <label class="form__label">{{ i18n(attribute.name) }}{% if required %} <span class="required">*</span>{% endif %}
        {% if i18nExists('attribute_hint_' ~ attribute.id ) %}
            {% set cmsContent = cms_content('attribute_hint_' ~ attribute.name ~ '_' ~ meta.language.iso) %}
            {% include 'Frontend/Components/Insertions/Hint.twig' with {
                hint: i18n('attribute_hint_' ~ attribute.id),
                hintContent: cmsContent} %}
        {% endif %}
    </label>
    <label for="insertion[{{ attribute.id }}"
           class="form__input form__input--inside-label">
        <input type="number"
               name="insertion[{{ attribute.id }}]"
               value="{{ data['insertion'][attribute.id] }}"
               min="0"
               class="form__input"
               placeholder="{{ i18n(attribute.name) }}"
               {% if required %}required{% endif %}
            {% block input__attributes %}
            {% endblock input__attributes %}
        />

        {% set unit = i18n('unit_' ~  attribute.name, '') %}
        {% if unit is not empty %}
            <span class="input__unit">{{ unit }}</span>
        {% endif %}
    </label>
</div>
