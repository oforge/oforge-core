<div class="attribute">
    {{ dump(attribute) }}
    <span class="attribute-label"style="font-weight: 700;">{{ attribute.name }}</span>
    <span class="attribute-value">{{ attribute.value }}</span>
</div>
