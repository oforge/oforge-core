{% block pageheader_logo_part %}
    <a href="{{ url("backend_dashboard") }}" class="logo">
        <!-- mini logo for sidebar mini 50x50 pixels -->
        <span class="logo-mini"> {{ config("backend.project.short") }}</span>
        <!-- logo for regular state and mobile devices -->
        <span class="logo-lg">{{ config("backend.project.name") }}</span>
    </a>
{% endblock %}