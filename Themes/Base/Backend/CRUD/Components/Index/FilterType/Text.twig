<input title="{{ crudFilterTitle }}" placeholder="{{ crudFilterTitle }}" name="{{ crudPropertyName }}"
       type="text" class="form-control" value="{{ meta.route.query[crudPropertyName]|default() }}" />
