<input title="{{ propertyName|capitalize }}" placeholder="{{ propertyName|capitalize }}"
       name="{{ propertyName }}"
       type="text" class="form-control" value="{{ route.query[propertyName]|default() }}" />
