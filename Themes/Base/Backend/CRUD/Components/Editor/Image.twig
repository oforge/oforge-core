{% include 'Backend/Components/Image.twig' with {
    name: 'data[' ~ (crud.context == 'index' ? (crudItem.id ~ '][') : '') ~ crudProperty.name ~ ']',
    data: crudItem[crudProperty.name],
} only %}
