{% include 'Backend/Components/Form/SingleImageUploader/Field.twig' with {
    singleImageUploader: {
        id: 'image-uploader-' ~ (crud.context == 'index' ? (item.id ~ '-') : '') ~ property.name,
        name: 'data[' ~ (crud.context == 'index' ? (item.id ~ '][') : '') ~ property.name ~ ']',
        value: item[property.name] ? media(item[property.name], 300) : null,
    }
}%}


