{% include 'Backend/CRUD/Components/Editor/Input.twig' with {
    crudFieldEditor: {
        type: 'text',
        pattern: crudProperty.editor.pattern|default(''),
        maxlength: crudProperty.editor.maxlength|default(''),
    }
} %}
