{% include 'Backend/CRUD/Components/Editor/Input.twig' with {
    crudFieldEditor: {
        type: 'text',
        pattern: crudProperty.editor.pattern|default(null),
        maxlength: crudProperty.editor.maxlength|default(null),
    }
} %}
