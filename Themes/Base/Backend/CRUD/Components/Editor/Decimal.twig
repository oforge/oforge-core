{% include 'Backend/CRUD/Components/Editor/Input.twig' with {
    crudFieldEditor: {
        type: 'number',
        min: crudProperty.editor.min|default(''),
        max: crudProperty.editor.max|default(''),
        step: crudProperty.editor.step|default('0.01'),
    }
} %}
